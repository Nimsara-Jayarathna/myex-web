// Placeholder for future dashboard-level filters.
// Currently the dashboard uses per-view filters (e.g. AllTransactions filters).
// This component is ready to host global filters if needed.
export const FilterBar = () => {
  return null
}

